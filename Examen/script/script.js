let salario
let horasT
const horasE = 30000
const horas = 20000

function CantHora(params) {
    horas = Number(prompt('Ingrese cantidad de horas trabajadas'))
}
CantHora()

function calcular(salario) {
    if (horasT >= 1 && horasT <= 40) {
        salario = CantHora * horas
    }
    else if (
        Extra = horasT - 40)
}

